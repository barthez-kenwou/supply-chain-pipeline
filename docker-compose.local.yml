# Local override: publish a host port; create a local bridge if needed.
services:
  web:
    ports:
      - "${WEB_HOST_PORT:-8080}:8080"

networks:
  proxy:
    name: ${PROXY_NETWORK:-supply-chain-local}
    external: false
